[build]
  base    = "artifacts/ml-editing"
  command = "pnpm install && PORT=3000 BASE_PATH=/ pnpm run build"
  publish = "artifacts/ml-editing/dist/public"

[build.environment]
  NODE_VERSION = "20"
